# Telegram bot token from @BotFather. Required.
BOT_TOKEN=

# Comma-separated Telegram user IDs that should have admin access (optional).
# Example: 123456789,987654321
ADMINS=

# Maximum file size accepted from users, in megabytes. Defaults to 15.
MAX_FILE_MB=15

# Per-user rate limit for tool executions per minute. Defaults to 30.
RATE_LIMIT_PER_USER_PER_MIN=30

# Directory where persistent user data and generated artifacts are stored.
# This should match the volume mounted to the container at /data.
PERSIST_DIR=/data

# Optional Redis connection string (e.g. redis://redis:6379/0).
# Leave blank to disable Redis-backed features.
REDIS_URL=
